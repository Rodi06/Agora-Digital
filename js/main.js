const postsIndex='posts/index.json';const postListEl=document.getElementById('post-list');const yearEl=document.getElementById('year');if(yearEl)yearEl.textContent=new Date().getFullYear();fetch(postsIndex).then(r=>r.json()).then(data=>{const list=data.posts||data;list.forEach(p=>{const card=document.createElement('article');card.className='post-card';card.innerHTML=`<h4>${p.title}</h4><div class='post-meta'>${p.date} · ${(p.tags||[]).join(', ')}</div><p></p><p><a href='post.html?post=${encodeURIComponent('posts/'+p.file)}'>Leer ensayo completo →</a></p>`;postListEl.appendChild(card);});}).catch(err=>{if(postListEl)postListEl.innerHTML='<p>No pude cargar las publicaciones.</p>';console.error(err);});